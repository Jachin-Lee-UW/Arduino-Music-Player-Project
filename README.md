The goal of this project is to use an Arduino R3 microcontroller in conjunction with an IR remote control and receiver to select specific songs from a list of options. The list will be shown on an LCD display. The IR remote control should also be able to implement functions that would normally be expected of any music player, such as increasing/decreasing the volume, pausing the song, etc. These functions, when enacted, will also be displayed on the LCD display.
